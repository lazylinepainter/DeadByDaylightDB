<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/rosa_dbd</item>
        <item name="colorPrimaryDark">@color/rosa_dbd</item>
        <item name="colorAccent">@color/rosa_dbd</item>
    </style>

</resources>